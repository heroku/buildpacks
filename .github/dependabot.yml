version: 2
updates:
  - package-ecosystem: "bundler"
    directory: "/.rundoc-workspace"
    schedule:
      interval: "monthly"
    allow:
      # Update transitive dependencies too.
      - dependency-type: "all"
    groups:
      ruby-dependencies:
        update-types:
          - "minor"
          - "patch"
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    groups:
      github-actions:
        update-types:
          - "minor"
          - "patch"
